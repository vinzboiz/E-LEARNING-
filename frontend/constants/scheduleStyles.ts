import { StyleSheet } from "react-native";

export const scheduleStyles = StyleSheet.create({
  datePickerBtn: {
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 8,
    padding: 10,
    marginBottom: 10,
    backgroundColor: "#fff",
  },
});
