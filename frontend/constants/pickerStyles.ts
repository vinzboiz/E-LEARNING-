import { StyleSheet } from "react-native";

export const pickerStyles = StyleSheet.create({
  pickerWrapper: {
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 8,
    marginBottom: 15,
  },
});
